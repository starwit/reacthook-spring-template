apiVersion: v1
kind: Service
metadata:
  name: {{ include "reacthook-spring.fullname" . }}
  labels:
    {{- include "reacthook-spring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reacthook-spring.selectorLabels" . | nindent 4 }}
